export type { Evento } from "@/modules/events/types/event.types";

export interface EventoFormData {
  title: string;
  description: string;
  location: string;
  event_date: string;
  color: string;
}
